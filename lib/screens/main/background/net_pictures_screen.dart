import 'package:flutter/material.dart';

class NetPicturesScreen extends StatelessWidget {
  const NetPicturesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}